<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta name="viewport" content="width=device-width,initial-scale=1" />
		<title>Welcome to Stocky</title>
		<style>
			body {
				font-family: Inter, system-ui, Segoe UI, Roboto, Arial;
				margin: 0;
				color: #0b1220;
				background: #f6f8fb;
			}
			.container {
				max-width: 900px;
				margin: 48px auto;
				padding: 32px;
				background: #fff;
				border-radius: 16px;
				box-shadow: 0 6px 30px rgba(11, 18, 32, 0.08);
				text-align: center;
			}
			h1 {
				margin-top: 0;
				color: #111827;
				font-size: 2.2rem;
			}
			p {
				font-size: 1.1rem;
				line-height: 1.6;
				color: #374151;
			}
			.badge {
				display: inline-block;
				margin: 12px 0;
				padding: 6px 14px;
				background: #eef2ff;
				color: #3730a3;
				border-radius: 999px;
				font-weight: 600;
				font-size: 0.95rem;
			}
			.btn {
				display: inline-block;
				margin-top: 24px;
				padding: 12px 20px;
				background: #3730a3;
				color: #fff;
				border-radius: 8px;
				font-weight: 600;
				text-decoration: none;
				transition: background 0.2s;
			}
			.btn:hover {
				background: #2d2789;
			}
			.section {
				margin-top: 40px;
				text-align: left;
			}
			ul {
				color: #4b5563;
				line-height: 1.7;
			}
		</style>
	</head>
	<body>
		<div class="container">
			<h1>👋 Welcome to Stocky!</h1>
			<p class="badge">Your Rewarding Stocks Companion</p>
			<p>
				We're glad to have you here. Stocky is a simple way to reward
				users with real stock units — making every reward meaningful,
				valuable, and future-ready. 🚀
			</p>

			<div class="section">
				<h2>🔗 API Highlights</h2>
				<ul>
					<li>
						<strong>POST /reward</strong> — record a reward event
					</li>
					<li>
						<strong>GET /today-stocks/{userId}</strong> — today’s
						rewards for a user
					</li>
					<li>
						<strong>GET /historical-inr/{userId}</strong> — INR
						value history
					</li>
					<li>
						<strong>GET /stats/{userId}</strong> — portfolio stats &
						today’s totals
					</li>
					<li>
						<strong>GET /portfolio/{userId}</strong> — holdings with
						live INR value
					</li>
				</ul>
			</div>
		</div>
	</body>
</html>
